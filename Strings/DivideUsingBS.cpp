using namespace std;
#include <iostream>
#include <vector>
/* As we know that divisor* quotient +remainder = dividend
So to find the quotient we compare quotient* divisor with dividend
and store the nearest quotient */
int getQuotientUsingBS(int divisor, int dividend)
{
  int i = 0, j = dividend;
  while (i < j)
  {
  }
}

int main()
{
  int divisor = 7;
  int dividend = 29;
}